<% if can? :update, @category %>
	<h1>Editing category</h1>

	<%= render 'form' %>

	<%= link_to 'Show', @category %> |
	<%= link_to 'Back', categories_path %>
<% else %>
  <%= render 'storefront/notallowed' %>
<% end %>